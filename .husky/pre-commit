# Run lint to catch errors
npm run lint

# Automatically fix lint issues and add fixed files to staging
npm run lint:fix
git add .

# Format code and add formatted files to staging
npm run format
git add .